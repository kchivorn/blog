<div class="post_wrapper">
  <h2 class="title">
    <%= link_to post.title, post %>
  </h2>
  <p class="date"><small>Submitted on <%= post.created_at.strftime("%B, %d, %Y") %> </small></p>
  <p class="body"><%= post.body %></p>
</div>
